from enum import Enum


class ChartLocationOptions(Enum):
    """
    Enum to represent the two options of chart location, one chart will
    be in the left, and the other one will be in the right.
    """
    LEFT = 'left'
    RIGHT = 'right'
